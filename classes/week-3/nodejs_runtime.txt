Engines like v8(C++) Spider Monkey(rust) compile js into byte code(0,1) and these were used by browsers eventually someone pulled out the v8 engine and added somethings on top to create NODE.js  

NodeJS - It is a runtime that is used to run js code on the backend.
We can use for creating CLI's, ,making HTTP servers, video players, games
Bun difference from NodeJS.// New topic

What does an HTTP server do ?
- It exposes our algo to the world . It lets people request for and use our logic.  

Backend routes languages:-
1. Express
2. NextJs